# 🍕 Pizzaro — сервис онлайн-заказа пиццы

**Next.js 15 · React 19 · Prisma · PostgreSQL · Docker**

Полноценное full-stack приложение: клиентская часть, административная панель, система авторизации, меню с конфигуратором товаров и оформление заказов.

---

## 🚀 Быстрый запуск

Клонируйте проект:

```bash
git clone git@github.com:Alephod/Pizzaro-project.git
```

Создайте и заполните файл окружения:

```bash
cp .env.example .env
```

Установка зависимостей ui-библиотеки:
```
cd ui-library
npm install
```

Сборка ui-библиотеки
```
npm run build
```

Запустите инфраструктуру:

```bash
docker-compose up -d
```

Инициализируйте базу данных:

```bash
docker-compose exec frontend npx prisma migrate reset --force
# подтвердите вводом y
```

Инициализируйте администратора
```bash
docker-compose exec frontend npx prisma db seed
```



Открывайте:

* Клиентская часть: [http://localhost:3000](http://localhost:3000)
* Админ-панель: [http://localhost:3000/admin](http://localhost:3000/admin)

---

## 🔑 Данные администратора

```
Логин: admin
Пароль: supersecret
```

---

## 📦 Полезные команды

```bash
# Пересборка контейнеров
docker-compose up -d --build

# Применение миграций без очистки БД
docker-compose exec frontend npx prisma migrate dev

# Полный сброс БД и данных
docker-compose exec frontend npx prisma migrate reset --force

# Остановка всех контейнеров
docker-compose down

# Тестирование
npm run test
npm run test:coverage

# Проверка eslint
npm run lint
npm run lint:fix

# Сборка приложения
npm run build 
```

---

## 🏗 Стек технологий

* Next.js 15 
* React 19
* TypeScript
* Prisma ORM
* PostgreSQL
* NextAuth
* Docker Compose
* Собственная UI-библиотека

---

## 📘 Структура проекта

```
kursach/
│── frontend/                # приложение Next.js
│   ├── src/app             # маршруты, страницы и API
│   ├── prisma              # схема БД и миграции
│   ├── lib                 # вспомогательные модули
│   ├── types               # типы данных
│   └── components          # клиентские/серверные компоненты
│── ui-library/             # собственная UI-библиотека
│── docker-compose.yml
```

---

## 🛠 Функциональность

### Клиентская часть

* просмотр меню
* конфигуратор товаров (варианты, добавки, исключения)
* корзина (локальное хранение)
* оформление заказа
* управление профилем
* отслеживание статуса заказа

### Админ-панель

* управление разделами меню
* создание и редактирование товаров
* загрузка изображений (временное/постоянное хранилище)
* обновление статусов заказов
* оптимистичные обновления и модальные формы
